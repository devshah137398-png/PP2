#include <stdio.h>

struct student {
    int rollno; char name[20]; char department[40]; int year_of_joining; float score;
};

int main() {
    struct student s[5], temp;
    // Input logic here...
    for(int i = 0; i < 4; i++) {
        for(int j = i + 1; j < 5; j++) {
            if(s[i].score < s[j].score) {
                temp = s[i]; s[i] = s[j]; s[j] = temp;
            }
        }
    }
    return 0;
}